module PersamaanKuadrat where

--Definisi dan Spesifikasi
persamaanKuadrat :: Int -> Int -> Int -> Int -> Int
    --persamaanKuadrat a b c x menghitung hasil persamaan kuadrat
    --berdasarkan a b c x (bilangan bulat) yang diberikan

--Realisasi
persamaanKuadrat a b c x = a * x * x + b * x + c